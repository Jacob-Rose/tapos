{
  "name": "tapos",
  "version": "0.1.0",
  "description": "TAPOS - That Audio Project Organization System for Ableton Live",
  "main": "js/generated/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && electron-forge start",
    "dev": "npm run build && cross-env NODE_ENV=development electron-forge start",
    "safe": "npm run build && electron-forge start -- --safe-mode",
    "package": "electron-forge package",
    "make": "electron-forge make"
  },
  "author": "Jake Rose",
  "license": "ISC",
  "devDependencies": {
    "@electron-forge/cli": "^6.1.1",
    "@electron-forge/maker-deb": "^6.1.1",
    "@electron-forge/maker-rpm": "^6.1.1",
    "@electron-forge/maker-squirrel": "^6.1.1",
    "@electron-forge/maker-zip": "^6.1.1",
    "@types/node": "^20.0.0",
    "cross-env": "^10.1.0",
    "electron": "^39.2.4",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "electron-store": "^8.1.0",
    "fast-xml-parser": "^4.2.0",
    "xml2js": "^0.5.0",
    "zlib": "^1.0.5"
  }
}
